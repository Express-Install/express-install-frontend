version: '3'

services:
  react-app:
    build: .
    image: react-app
    ports:
      - '4000:4000'
    volumes:
      - .:/home/node/react-app:rw
    networks:
      - react-network

volumes:
  dbdata:

networks:
  react-network:
    driver: bridge
